# Routing identifiers for the Chainsaw Consultant dynamic instance.

from toontown.instances import InstanceGlobals

CHAINSAW = InstanceGlobals.CHAINSAW
INSTANCE_LOADER = 'townLoader'
BOSS_BATTLE_STATE = 'chainsawBossBattle'

# Clash-authored battle origin.
BATTLE_NODE_POS_HPR = (0, 10, 0, 180, 0, 0)

# Used only if the authored room entrance locator is unavailable.
TELEPORT_IN_POS_HPR = (0, -12, 0.05, 0, 0, 0)
