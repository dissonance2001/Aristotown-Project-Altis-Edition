# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


from typing_extensions import LiteralString
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class StaffUserActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StaffUser]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = StaffUser.prisma().query_raw(
            'SELECT * FROM StaffUser WHERE id = $1',
            1103527590,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StaffUser
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = StaffUser.prisma().query_first(
            'SELECT * FROM StaffUser WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.StaffUserCreateInput,
        include: Optional[types.StaffUserInclude] = None
    ) -> _PrismaModelT:
        """Create a new StaffUser record.

        Parameters
        ----------
        data
            StaffUser record data
        include
            Specifies which relations should be loaded on the returned StaffUser model

        Returns
        -------
        prisma.models.StaffUser
            The created StaffUser record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StaffUser record from just the required fields
        staffuser = StaffUser.prisma().create(
            data={
                # data to create a StaffUser record
                'name': 'ggciceaie',
                'email': 'bbehjachib',
                'googleAccessToken': 'cadfabfehe',
                'googleRefreshToken': 'dgiiaaijj',
                'googleIdToken': 'bfaiacjjfc',
                'profilePicture': 'eigcfgbif',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.StaffUserCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StaffUser records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StaffUser record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = StaffUser.prisma().create_many(
            data=[
                {
                    # data to create a StaffUser record
                    'name': 'bagcfbhiig',
                    'email': 'cghideieh',
                    'googleAccessToken': 'biabhbdai',
                    'googleRefreshToken': 'idghgaicb',
                    'googleIdToken': 'fjfddhigg',
                    'profilePicture': 'hjaecfifb',
                },
                {
                    # data to create a StaffUser record
                    'name': 'cbbbjbfcii',
                    'email': 'bbejhfidcb',
                    'googleAccessToken': 'bgeecijdgg',
                    'googleRefreshToken': 'bdiicjafbj',
                    'googleIdToken': 'bgehebiafc',
                    'profilePicture': 'bghffegacj',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.StaffUserWhereUniqueInput,
        include: Optional[types.StaffUserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StaffUser record.

        Parameters
        ----------
        where
            StaffUser filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StaffUser model

        Returns
        -------
        prisma.models.StaffUser
            The deleted StaffUser record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuser = StaffUser.prisma().delete(
            where={
                'id': 1767274722,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.StaffUserWhereUniqueInput,
        include: Optional[types.StaffUserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StaffUser record.

        Parameters
        ----------
        where
            StaffUser filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StaffUser model

        Returns
        -------
        prisma.models.StaffUser
            The found StaffUser record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuser = StaffUser.prisma().find_unique(
            where={
                'id': 326272115,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.StaffUserWhereUniqueInput,
        include: Optional[types.StaffUserInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StaffUser record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StaffUser filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StaffUser model

        Returns
        -------
        prisma.models.StaffUser
            The found StaffUser record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuser = StaffUser.prisma().find_unique_or_raise(
            where={
                'id': 1343201072,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserWhereInput] = None,
        cursor: Optional[types.StaffUserWhereUniqueInput] = None,
        include: Optional[types.StaffUserInclude] = None,
        order: Optional[Union[types.StaffUserOrderByInput, List[types.StaffUserOrderByInput]]] = None,
        distinct: Optional[List[types.StaffUserScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StaffUser records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StaffUser records returned
        skip
            Ignore the first N results
        where
            StaffUser filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StaffUser model
        order
            Order the returned StaffUser records by any field
        distinct
            Filter StaffUser records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StaffUser]
            The list of all StaffUser records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StaffUser records
        staffusers = StaffUser.prisma().find_many(take=10)

        # find the first 5 StaffUser records ordered by the email field
        staffusers = StaffUser.prisma().find_many(
            take=5,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserWhereInput] = None,
        cursor: Optional[types.StaffUserWhereUniqueInput] = None,
        include: Optional[types.StaffUserInclude] = None,
        order: Optional[Union[types.StaffUserOrderByInput, List[types.StaffUserOrderByInput]]] = None,
        distinct: Optional[List[types.StaffUserScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StaffUser record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StaffUser filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StaffUser model
        order
            Order the returned StaffUser records by any field
        distinct
            Filter StaffUser records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StaffUser
            The first StaffUser record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StaffUser record ordered by the googleAccessToken field
        staffuser = StaffUser.prisma().find_first(
            skip=1,
            order={
                'googleAccessToken': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserWhereInput] = None,
        cursor: Optional[types.StaffUserWhereUniqueInput] = None,
        include: Optional[types.StaffUserInclude] = None,
        order: Optional[Union[types.StaffUserOrderByInput, List[types.StaffUserOrderByInput]]] = None,
        distinct: Optional[List[types.StaffUserScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StaffUser record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StaffUser filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StaffUser model
        order
            Order the returned StaffUser records by any field
        distinct
            Filter StaffUser records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StaffUser
            The first StaffUser record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StaffUser record ordered by the googleRefreshToken field
        staffuser = StaffUser.prisma().find_first_or_raise(
            skip=1,
            order={
                'googleRefreshToken': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.StaffUserUpdateInput,
        where: types.StaffUserWhereUniqueInput,
        include: Optional[types.StaffUserInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StaffUser record.

        Parameters
        ----------
        data
            StaffUser record data specifying what to update
        where
            StaffUser filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StaffUser model

        Returns
        -------
        prisma.models.StaffUser
            The updated StaffUser record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        staffuser = StaffUser.prisma().update(
            where={
                'id': 675780521,
            },
            data={
                # data to update the StaffUser record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.StaffUserWhereUniqueInput,
        data: types.StaffUserUpsertInput,
        include: Optional[types.StaffUserInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StaffUser filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StaffUser model

        Returns
        -------
        prisma.models.StaffUser
            The created or updated StaffUser record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuser = StaffUser.prisma().upsert(
            where={
                'id': 744964398,
            },
            data={
                'create': {
                    'id': 744964398,
                    'name': 'cbbbjbfcii',
                    'email': 'bbejhfidcb',
                    'googleAccessToken': 'bgeecijdgg',
                    'googleRefreshToken': 'bdiicjafbj',
                    'googleIdToken': 'bgehebiafc',
                    'profilePicture': 'bghffegacj',
                },
                'update': {
                    'name': 'cbbbjbfcii',
                    'email': 'bbejhfidcb',
                    'googleAccessToken': 'bgeecijdgg',
                    'googleRefreshToken': 'bdiicjafbj',
                    'googleIdToken': 'bgehebiafc',
                    'profilePicture': 'bghffegacj',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.StaffUserUpdateManyMutationInput,
        where: types.StaffUserWhereInput,
    ) -> int:
        """Update multiple StaffUser records

        Parameters
        ----------
        data
            StaffUser data to update the selected StaffUser records to
        where
            Filter to select the StaffUser records to update

        Returns
        -------
        int
            The total number of StaffUser records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StaffUser records
        total = StaffUser.prisma().update_many(
            data={
                'googleIdToken': 'bjgjgibgbf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserWhereInput] = None,
        cursor: Optional[types.StaffUserWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StaffUser records present in the database

        Parameters
        ----------
        select
            Select the StaffUser fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StaffUser filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StaffUserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StaffUser.prisma().count()

        # results: prisma.types.StaffUserCountAggregateOutput
        results = StaffUser.prisma().count(
            select={
                '_all': True,
                'profilePicture': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.StaffUserCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserWhereInput] = None,
        cursor: Optional[types.StaffUserWhereUniqueInput] = None,
    ) -> types.StaffUserCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.StaffUserCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserWhereInput] = None,
        cursor: Optional[types.StaffUserWhereUniqueInput] = None,
    ) -> Union[int, types.StaffUserCountAggregateOutput]:
        """Count the number of StaffUser records present in the database

        Parameters
        ----------
        select
            Select the StaffUser fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StaffUser filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StaffUserCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StaffUser.prisma().count()

        # results: prisma.types.StaffUserCountAggregateOutput
        results = StaffUser.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StaffUserCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.StaffUserWhereInput] = None
    ) -> int:
        """Delete multiple StaffUser records.

        Parameters
        ----------
        where
            Optional StaffUser filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StaffUser records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StaffUser records
        total = StaffUser.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.StaffUserScalarFieldKeysT'],
        *,
        where: Optional['types.StaffUserWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StaffUserAvgAggregateInput'] = None,
        sum: Optional['types.StaffUserSumAggregateInput'] = None,
        min: Optional['types.StaffUserMinAggregateInput'] = None,
        max: Optional['types.StaffUserMaxAggregateInput'] = None,
        having: Optional['types.StaffUserScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StaffUserCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StaffUserScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.StaffUserScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.StaffUserGroupByOutput']:
        """Group StaffUser records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StaffUser fields to group records by
        where
            StaffUser filter to select records
        take
            Limit the maximum number of StaffUser records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StaffUserGroupByOutput]
            A list of dictionaries representing the StaffUser record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StaffUser records by updatedAt values
        # and count how many records are in each group
        results = StaffUser.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StaffUserLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StaffUserLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = StaffUserLog.prisma().query_raw(
            'SELECT * FROM StaffUserLog WHERE id = $1',
            1116175964,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StaffUserLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = StaffUserLog.prisma().query_first(
            'SELECT * FROM StaffUserLog WHERE staffUserId = $1',
            861472101,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.StaffUserLogCreateInput,
        include: Optional[types.StaffUserLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new StaffUserLog record.

        Parameters
        ----------
        data
            StaffUserLog record data
        include
            Specifies which relations should be loaded on the returned StaffUserLog model

        Returns
        -------
        prisma.models.StaffUserLog
            The created StaffUserLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StaffUserLog record from just the required fields
        staffuserlog = StaffUserLog.prisma().create(
            data={
                # data to create a StaffUserLog record
                'createdById': 1303003706,
                'logType': enums.StaffUserLogType.STAFF_EDITED,
                'notes': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.StaffUserLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StaffUserLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StaffUserLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = StaffUserLog.prisma().create_many(
            data=[
                {
                    # data to create a StaffUserLog record
                    'createdById': 2000430152,
                    'logType': enums.StaffUserLogType.STAFF_EDITED,
                    'notes': 'bigibebcib',
                },
                {
                    # data to create a StaffUserLog record
                    'createdById': 1860847622,
                    'logType': enums.StaffUserLogType.STAFF_EDITED,
                    'notes': 'beeifcbebf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.StaffUserLogWhereUniqueInput,
        include: Optional[types.StaffUserLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StaffUserLog record.

        Parameters
        ----------
        where
            StaffUserLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StaffUserLog model

        Returns
        -------
        prisma.models.StaffUserLog
            The deleted StaffUserLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuserlog = StaffUserLog.prisma().delete(
            where={
                'id': 1628650740,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.StaffUserLogWhereUniqueInput,
        include: Optional[types.StaffUserLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StaffUserLog record.

        Parameters
        ----------
        where
            StaffUserLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StaffUserLog model

        Returns
        -------
        prisma.models.StaffUserLog
            The found StaffUserLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuserlog = StaffUserLog.prisma().find_unique(
            where={
                'id': 1249606685,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.StaffUserLogWhereUniqueInput,
        include: Optional[types.StaffUserLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StaffUserLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StaffUserLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StaffUserLog model

        Returns
        -------
        prisma.models.StaffUserLog
            The found StaffUserLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuserlog = StaffUserLog.prisma().find_unique_or_raise(
            where={
                'id': 835903122,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserLogWhereInput] = None,
        cursor: Optional[types.StaffUserLogWhereUniqueInput] = None,
        include: Optional[types.StaffUserLogInclude] = None,
        order: Optional[Union[types.StaffUserLogOrderByInput, List[types.StaffUserLogOrderByInput]]] = None,
        distinct: Optional[List[types.StaffUserLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StaffUserLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StaffUserLog records returned
        skip
            Ignore the first N results
        where
            StaffUserLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StaffUserLog model
        order
            Order the returned StaffUserLog records by any field
        distinct
            Filter StaffUserLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StaffUserLog]
            The list of all StaffUserLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StaffUserLog records
        staffuserlogs = StaffUserLog.prisma().find_many(take=10)

        # find the first 5 StaffUserLog records ordered by the createdAt field
        staffuserlogs = StaffUserLog.prisma().find_many(
            take=5,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserLogWhereInput] = None,
        cursor: Optional[types.StaffUserLogWhereUniqueInput] = None,
        include: Optional[types.StaffUserLogInclude] = None,
        order: Optional[Union[types.StaffUserLogOrderByInput, List[types.StaffUserLogOrderByInput]]] = None,
        distinct: Optional[List[types.StaffUserLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StaffUserLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StaffUserLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StaffUserLog model
        order
            Order the returned StaffUserLog records by any field
        distinct
            Filter StaffUserLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StaffUserLog
            The first StaffUserLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StaffUserLog record ordered by the createdById field
        staffuserlog = StaffUserLog.prisma().find_first(
            skip=1,
            order={
                'createdById': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserLogWhereInput] = None,
        cursor: Optional[types.StaffUserLogWhereUniqueInput] = None,
        include: Optional[types.StaffUserLogInclude] = None,
        order: Optional[Union[types.StaffUserLogOrderByInput, List[types.StaffUserLogOrderByInput]]] = None,
        distinct: Optional[List[types.StaffUserLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StaffUserLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StaffUserLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StaffUserLog model
        order
            Order the returned StaffUserLog records by any field
        distinct
            Filter StaffUserLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StaffUserLog
            The first StaffUserLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StaffUserLog record ordered by the logType field
        staffuserlog = StaffUserLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'logType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.StaffUserLogUpdateInput,
        where: types.StaffUserLogWhereUniqueInput,
        include: Optional[types.StaffUserLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StaffUserLog record.

        Parameters
        ----------
        data
            StaffUserLog record data specifying what to update
        where
            StaffUserLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StaffUserLog model

        Returns
        -------
        prisma.models.StaffUserLog
            The updated StaffUserLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        staffuserlog = StaffUserLog.prisma().update(
            where={
                'id': 763719779,
            },
            data={
                # data to update the StaffUserLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.StaffUserLogWhereUniqueInput,
        data: types.StaffUserLogUpsertInput,
        include: Optional[types.StaffUserLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StaffUserLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StaffUserLog model

        Returns
        -------
        prisma.models.StaffUserLog
            The created or updated StaffUserLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        staffuserlog = StaffUserLog.prisma().upsert(
            where={
                'id': 429995104,
            },
            data={
                'create': {
                    'id': 429995104,
                    'createdById': 1860847622,
                    'logType': enums.StaffUserLogType.STAFF_EDITED,
                    'notes': 'beeifcbebf',
                },
                'update': {
                    'createdById': 1860847622,
                    'logType': enums.StaffUserLogType.STAFF_EDITED,
                    'notes': 'beeifcbebf',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.StaffUserLogUpdateManyMutationInput,
        where: types.StaffUserLogWhereInput,
    ) -> int:
        """Update multiple StaffUserLog records

        Parameters
        ----------
        data
            StaffUserLog data to update the selected StaffUserLog records to
        where
            Filter to select the StaffUserLog records to update

        Returns
        -------
        int
            The total number of StaffUserLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StaffUserLog records
        total = StaffUserLog.prisma().update_many(
            data={
                'notes': 'bhhfibbigf'
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserLogWhereInput] = None,
        cursor: Optional[types.StaffUserLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StaffUserLog records present in the database

        Parameters
        ----------
        select
            Select the StaffUserLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StaffUserLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StaffUserLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StaffUserLog.prisma().count()

        # results: prisma.types.StaffUserLogCountAggregateOutput
        results = StaffUserLog.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.StaffUserLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserLogWhereInput] = None,
        cursor: Optional[types.StaffUserLogWhereUniqueInput] = None,
    ) -> types.StaffUserLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.StaffUserLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StaffUserLogWhereInput] = None,
        cursor: Optional[types.StaffUserLogWhereUniqueInput] = None,
    ) -> Union[int, types.StaffUserLogCountAggregateOutput]:
        """Count the number of StaffUserLog records present in the database

        Parameters
        ----------
        select
            Select the StaffUserLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StaffUserLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StaffUserLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = StaffUserLog.prisma().count()

        # results: prisma.types.StaffUserLogCountAggregateOutput
        results = StaffUserLog.prisma().count(
            select={
                '_all': True,
                'staffUserId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StaffUserLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.StaffUserLogWhereInput] = None
    ) -> int:
        """Delete multiple StaffUserLog records.

        Parameters
        ----------
        where
            Optional StaffUserLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StaffUserLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StaffUserLog records
        total = StaffUserLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.StaffUserLogScalarFieldKeysT'],
        *,
        where: Optional['types.StaffUserLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StaffUserLogAvgAggregateInput'] = None,
        sum: Optional['types.StaffUserLogSumAggregateInput'] = None,
        min: Optional['types.StaffUserLogMinAggregateInput'] = None,
        max: Optional['types.StaffUserLogMaxAggregateInput'] = None,
        having: Optional['types.StaffUserLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StaffUserLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StaffUserLogScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.StaffUserLogScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.StaffUserLogGroupByOutput']:
        """Group StaffUserLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StaffUserLog fields to group records by
        where
            StaffUserLog filter to select records
        take
            Limit the maximum number of StaffUserLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StaffUserLogGroupByOutput]
            A list of dictionaries representing the StaffUserLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StaffUserLog records by createdAt values
        # and count how many records are in each group
        results = StaffUserLog.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ClubActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Club]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = Club.prisma().query_raw(
            'SELECT * FROM Club WHERE id = $1',
            893145566,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Club
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = Club.prisma().query_first(
            'SELECT * FROM Club WHERE name = $1',
            'jjfeafhfj',
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ClubCreateInput,
        include: Optional[types.ClubInclude] = None
    ) -> _PrismaModelT:
        """Create a new Club record.

        Parameters
        ----------
        data
            Club record data
        include
            Specifies which relations should be loaded on the returned Club model

        Returns
        -------
        prisma.models.Club
            The created Club record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Club record from just the required fields
        club = Club.prisma().create(
            data={
                # data to create a Club record
                'name': 'cbachdgfce',
                'ownerAccountId': 271520213,
                'ownerAvId': 456633834,
                'clubIcon': Json({'caficfigfb': True}),
                'capacity': 1583689592,
                'dailyCoins': 878442065,
                'clubXp': 1675280054,
                'clubCoins': 1627576247.205480,
                'jellybeans': 60335757,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ClubCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Club records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Club record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = Club.prisma().create_many(
            data=[
                {
                    # data to create a Club record
                    'name': 'gieegcbeg',
                    'ownerAccountId': 1625503827,
                    'ownerAvId': 521827728,
                    'clubIcon': Json({'bcggadccgf': True}),
                    'capacity': 93253262,
                    'dailyCoins': 2053047983,
                    'clubXp': 685333180,
                    'clubCoins': 127474245.94892,
                    'jellybeans': 1964990155,
                },
                {
                    # data to create a Club record
                    'name': 'bcciijbibg',
                    'ownerAccountId': 255202753,
                    'ownerAvId': 1223573862,
                    'clubIcon': Json({'febcgjbfj': True}),
                    'capacity': 1064846676,
                    'dailyCoins': 508382461,
                    'clubXp': 1024265714,
                    'clubCoins': 872078403.187474,
                    'jellybeans': 916896761,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ClubWhereUniqueInput,
        include: Optional[types.ClubInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Club record.

        Parameters
        ----------
        where
            Club filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Club model

        Returns
        -------
        prisma.models.Club
            The deleted Club record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        club = Club.prisma().delete(
            where={
                'id': 769267518,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ClubWhereUniqueInput,
        include: Optional[types.ClubInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Club record.

        Parameters
        ----------
        where
            Club filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Club model

        Returns
        -------
        prisma.models.Club
            The found Club record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        club = Club.prisma().find_unique(
            where={
                'id': 820312479,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ClubWhereUniqueInput,
        include: Optional[types.ClubInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Club record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Club filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Club model

        Returns
        -------
        prisma.models.Club
            The found Club record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        club = Club.prisma().find_unique_or_raise(
            where={
                'id': 92728044,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubWhereInput] = None,
        cursor: Optional[types.ClubWhereUniqueInput] = None,
        include: Optional[types.ClubInclude] = None,
        order: Optional[Union[types.ClubOrderByInput, List[types.ClubOrderByInput]]] = None,
        distinct: Optional[List[types.ClubScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Club records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Club records returned
        skip
            Ignore the first N results
        where
            Club filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Club model
        order
            Order the returned Club records by any field
        distinct
            Filter Club records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Club]
            The list of all Club records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Club records
        clubs = Club.prisma().find_many(take=10)

        # find the first 5 Club records ordered by the requestedName field
        clubs = Club.prisma().find_many(
            take=5,
            order={
                'requestedName': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubWhereInput] = None,
        cursor: Optional[types.ClubWhereUniqueInput] = None,
        include: Optional[types.ClubInclude] = None,
        order: Optional[Union[types.ClubOrderByInput, List[types.ClubOrderByInput]]] = None,
        distinct: Optional[List[types.ClubScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Club record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Club filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Club model
        order
            Order the returned Club records by any field
        distinct
            Filter Club records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Club
            The first Club record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Club record ordered by the nameRequestId field
        club = Club.prisma().find_first(
            skip=1,
            order={
                'nameRequestId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubWhereInput] = None,
        cursor: Optional[types.ClubWhereUniqueInput] = None,
        include: Optional[types.ClubInclude] = None,
        order: Optional[Union[types.ClubOrderByInput, List[types.ClubOrderByInput]]] = None,
        distinct: Optional[List[types.ClubScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Club record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Club filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Club model
        order
            Order the returned Club records by any field
        distinct
            Filter Club records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Club
            The first Club record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Club record ordered by the nameStatus field
        club = Club.prisma().find_first_or_raise(
            skip=1,
            order={
                'nameStatus': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ClubUpdateInput,
        where: types.ClubWhereUniqueInput,
        include: Optional[types.ClubInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Club record.

        Parameters
        ----------
        data
            Club record data specifying what to update
        where
            Club filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Club model

        Returns
        -------
        prisma.models.Club
            The updated Club record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        club = Club.prisma().update(
            where={
                'id': 344858293,
            },
            data={
                # data to update the Club record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ClubWhereUniqueInput,
        data: types.ClubUpsertInput,
        include: Optional[types.ClubInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Club filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Club model

        Returns
        -------
        prisma.models.Club
            The created or updated Club record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        club = Club.prisma().upsert(
            where={
                'id': 1121741130,
            },
            data={
                'create': {
                    'id': 1121741130,
                    'name': 'bcciijbibg',
                    'ownerAccountId': 255202753,
                    'ownerAvId': 1223573862,
                    'clubIcon': Json({'febcgjbfj': True}),
                    'capacity': 1064846676,
                    'dailyCoins': 508382461,
                    'clubXp': 1024265714,
                    'clubCoins': 872078403.187474,
                    'jellybeans': 916896761,
                },
                'update': {
                    'name': 'bcciijbibg',
                    'ownerAccountId': 255202753,
                    'ownerAvId': 1223573862,
                    'clubIcon': Json({'febcgjbfj': True}),
                    'capacity': 1064846676,
                    'dailyCoins': 508382461,
                    'clubXp': 1024265714,
                    'clubCoins': 872078403.187474,
                    'jellybeans': 916896761,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ClubUpdateManyMutationInput,
        where: types.ClubWhereInput,
    ) -> int:
        """Update multiple Club records

        Parameters
        ----------
        data
            Club data to update the selected Club records to
        where
            Filter to select the Club records to update

        Returns
        -------
        int
            The total number of Club records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Club records
        total = Club.prisma().update_many(
            data={
                'ownerAccountId': 1495896251
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubWhereInput] = None,
        cursor: Optional[types.ClubWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Club records present in the database

        Parameters
        ----------
        select
            Select the Club fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Club filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Club.prisma().count()

        # results: prisma.types.ClubCountAggregateOutput
        results = Club.prisma().count(
            select={
                '_all': True,
                'ownerAvId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ClubCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubWhereInput] = None,
        cursor: Optional[types.ClubWhereUniqueInput] = None,
    ) -> types.ClubCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ClubCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubWhereInput] = None,
        cursor: Optional[types.ClubWhereUniqueInput] = None,
    ) -> Union[int, types.ClubCountAggregateOutput]:
        """Count the number of Club records present in the database

        Parameters
        ----------
        select
            Select the Club fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Club filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = Club.prisma().count()

        # results: prisma.types.ClubCountAggregateOutput
        results = Club.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ClubCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ClubWhereInput] = None
    ) -> int:
        """Delete multiple Club records.

        Parameters
        ----------
        where
            Optional Club filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Club records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Club records
        total = Club.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ClubScalarFieldKeysT'],
        *,
        where: Optional['types.ClubWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ClubAvgAggregateInput'] = None,
        sum: Optional['types.ClubSumAggregateInput'] = None,
        min: Optional['types.ClubMinAggregateInput'] = None,
        max: Optional['types.ClubMaxAggregateInput'] = None,
        having: Optional['types.ClubScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ClubCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ClubScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.ClubScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.ClubGroupByOutput']:
        """Group Club records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Club fields to group records by
        where
            Club filter to select records
        take
            Limit the maximum number of Club records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ClubGroupByOutput]
            A list of dictionaries representing the Club record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Club records by updatedAt values
        # and count how many records are in each group
        results = Club.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ClubToonActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ClubToon]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ClubToon.prisma().query_raw(
            'SELECT * FROM ClubToon WHERE id = $1',
            208521688,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ClubToon
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ClubToon.prisma().query_first(
            'SELECT * FROM ClubToon WHERE clubId = $1',
            860811569,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ClubToonCreateInput,
        include: Optional[types.ClubToonInclude] = None
    ) -> _PrismaModelT:
        """Create a new ClubToon record.

        Parameters
        ----------
        data
            ClubToon record data
        include
            Specifies which relations should be loaded on the returned ClubToon model

        Returns
        -------
        prisma.models.ClubToon
            The created ClubToon record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ClubToon record from just the required fields
        clubtoon = ClubToon.prisma().create(
            data={
                # data to create a ClubToon record
                'clubId': 1660932118,
                'avId': 525761943,
                'invitedBy': 736209796,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ClubToonCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ClubToon records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ClubToon record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ClubToon.prisma().create_many(
            data=[
                {
                    # data to create a ClubToon record
                    'clubId': 493907821,
                    'avId': 639686562,
                    'invitedBy': 654007347,
                },
                {
                    # data to create a ClubToon record
                    'clubId': 1905261552,
                    'avId': 78746985,
                    'invitedBy': 1398328302,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ClubToonWhereUniqueInput,
        include: Optional[types.ClubToonInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ClubToon record.

        Parameters
        ----------
        where
            ClubToon filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ClubToon model

        Returns
        -------
        prisma.models.ClubToon
            The deleted ClubToon record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubtoon = ClubToon.prisma().delete(
            where={
                'id': 856000655,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ClubToonWhereUniqueInput,
        include: Optional[types.ClubToonInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ClubToon record.

        Parameters
        ----------
        where
            ClubToon filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubToon model

        Returns
        -------
        prisma.models.ClubToon
            The found ClubToon record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubtoon = ClubToon.prisma().find_unique(
            where={
                'id': 1452336924,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ClubToonWhereUniqueInput,
        include: Optional[types.ClubToonInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ClubToon record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ClubToon filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubToon model

        Returns
        -------
        prisma.models.ClubToon
            The found ClubToon record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubtoon = ClubToon.prisma().find_unique_or_raise(
            where={
                'id': 1573199653,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubToonWhereInput] = None,
        cursor: Optional[types.ClubToonWhereUniqueInput] = None,
        include: Optional[types.ClubToonInclude] = None,
        order: Optional[Union[types.ClubToonOrderByInput, List[types.ClubToonOrderByInput]]] = None,
        distinct: Optional[List[types.ClubToonScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ClubToon records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ClubToon records returned
        skip
            Ignore the first N results
        where
            ClubToon filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubToon model
        order
            Order the returned ClubToon records by any field
        distinct
            Filter ClubToon records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ClubToon]
            The list of all ClubToon records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ClubToon records
        clubtoons = ClubToon.prisma().find_many(take=10)

        # find the first 5 ClubToon records ordered by the avId field
        clubtoons = ClubToon.prisma().find_many(
            take=5,
            order={
                'avId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubToonWhereInput] = None,
        cursor: Optional[types.ClubToonWhereUniqueInput] = None,
        include: Optional[types.ClubToonInclude] = None,
        order: Optional[Union[types.ClubToonOrderByInput, List[types.ClubToonOrderByInput]]] = None,
        distinct: Optional[List[types.ClubToonScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ClubToon record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubToon filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubToon model
        order
            Order the returned ClubToon records by any field
        distinct
            Filter ClubToon records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubToon
            The first ClubToon record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubToon record ordered by the invitedBy field
        clubtoon = ClubToon.prisma().find_first(
            skip=1,
            order={
                'invitedBy': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubToonWhereInput] = None,
        cursor: Optional[types.ClubToonWhereUniqueInput] = None,
        include: Optional[types.ClubToonInclude] = None,
        order: Optional[Union[types.ClubToonOrderByInput, List[types.ClubToonOrderByInput]]] = None,
        distinct: Optional[List[types.ClubToonScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ClubToon record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubToon filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubToon model
        order
            Order the returned ClubToon records by any field
        distinct
            Filter ClubToon records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubToon
            The first ClubToon record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubToon record ordered by the requestedAt field
        clubtoon = ClubToon.prisma().find_first_or_raise(
            skip=1,
            order={
                'requestedAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ClubToonUpdateInput,
        where: types.ClubToonWhereUniqueInput,
        include: Optional[types.ClubToonInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ClubToon record.

        Parameters
        ----------
        data
            ClubToon record data specifying what to update
        where
            ClubToon filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ClubToon model

        Returns
        -------
        prisma.models.ClubToon
            The updated ClubToon record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        clubtoon = ClubToon.prisma().update(
            where={
                'id': 2013903098,
            },
            data={
                # data to update the ClubToon record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ClubToonWhereUniqueInput,
        data: types.ClubToonUpsertInput,
        include: Optional[types.ClubToonInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ClubToon filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ClubToon model

        Returns
        -------
        prisma.models.ClubToon
            The created or updated ClubToon record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubtoon = ClubToon.prisma().upsert(
            where={
                'id': 500965035,
            },
            data={
                'create': {
                    'id': 500965035,
                    'clubId': 1905261552,
                    'avId': 78746985,
                    'invitedBy': 1398328302,
                },
                'update': {
                    'clubId': 1905261552,
                    'avId': 78746985,
                    'invitedBy': 1398328302,
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ClubToonUpdateManyMutationInput,
        where: types.ClubToonWhereInput,
    ) -> int:
        """Update multiple ClubToon records

        Parameters
        ----------
        data
            ClubToon data to update the selected ClubToon records to
        where
            Filter to select the ClubToon records to update

        Returns
        -------
        int
            The total number of ClubToon records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ClubToon records
        total = ClubToon.prisma().update_many(
            data={
                'joinedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubToonWhereInput] = None,
        cursor: Optional[types.ClubToonWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ClubToon records present in the database

        Parameters
        ----------
        select
            Select the ClubToon fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubToon filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubToonCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubToon.prisma().count()

        # results: prisma.types.ClubToonCountAggregateOutput
        results = ClubToon.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ClubToonCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubToonWhereInput] = None,
        cursor: Optional[types.ClubToonWhereUniqueInput] = None,
    ) -> types.ClubToonCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ClubToonCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubToonWhereInput] = None,
        cursor: Optional[types.ClubToonWhereUniqueInput] = None,
    ) -> Union[int, types.ClubToonCountAggregateOutput]:
        """Count the number of ClubToon records present in the database

        Parameters
        ----------
        select
            Select the ClubToon fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubToon filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubToonCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubToon.prisma().count()

        # results: prisma.types.ClubToonCountAggregateOutput
        results = ClubToon.prisma().count(
            select={
                '_all': True,
                'deletedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ClubToonCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ClubToonWhereInput] = None
    ) -> int:
        """Delete multiple ClubToon records.

        Parameters
        ----------
        where
            Optional ClubToon filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ClubToon records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ClubToon records
        total = ClubToon.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ClubToonScalarFieldKeysT'],
        *,
        where: Optional['types.ClubToonWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ClubToonAvgAggregateInput'] = None,
        sum: Optional['types.ClubToonSumAggregateInput'] = None,
        min: Optional['types.ClubToonMinAggregateInput'] = None,
        max: Optional['types.ClubToonMaxAggregateInput'] = None,
        having: Optional['types.ClubToonScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ClubToonCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ClubToonScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.ClubToonScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.ClubToonGroupByOutput']:
        """Group ClubToon records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ClubToon fields to group records by
        where
            ClubToon filter to select records
        take
            Limit the maximum number of ClubToon records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ClubToonGroupByOutput]
            A list of dictionaries representing the ClubToon record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ClubToon records by rankId values
        # and count how many records are in each group
        results = ClubToon.prisma().group_by(
            ['rankId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ClubLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ClubLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ClubLog.prisma().query_raw(
            'SELECT * FROM ClubLog WHERE id = $1',
            1800624392,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ClubLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ClubLog.prisma().query_first(
            'SELECT * FROM ClubLog WHERE clubId = $1',
            2077067425,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ClubLogCreateInput,
        include: Optional[types.ClubLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new ClubLog record.

        Parameters
        ----------
        data
            ClubLog record data
        include
            Specifies which relations should be loaded on the returned ClubLog model

        Returns
        -------
        prisma.models.ClubLog
            The created ClubLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ClubLog record from just the required fields
        clublog = ClubLog.prisma().create(
            data={
                # data to create a ClubLog record
                'clubId': 1672112838,
                'logType': enums.ClubLogType.CLUB_CREATED,
                'data': Json({'jcgghhgdj': True}),
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ClubLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ClubLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ClubLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ClubLog.prisma().create_many(
            data=[
                {
                    # data to create a ClubLog record
                    'clubId': 1447624116,
                    'logType': enums.ClubLogType.CLUB_CREATED,
                    'data': Json({'bhdiaidiaf': True}),
                },
                {
                    # data to create a ClubLog record
                    'clubId': 340946258,
                    'logType': enums.ClubLogType.CLUB_CREATED,
                    'data': Json({'gabahhhjf': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ClubLogWhereUniqueInput,
        include: Optional[types.ClubLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ClubLog record.

        Parameters
        ----------
        where
            ClubLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ClubLog model

        Returns
        -------
        prisma.models.ClubLog
            The deleted ClubLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clublog = ClubLog.prisma().delete(
            where={
                'id': 290603296,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ClubLogWhereUniqueInput,
        include: Optional[types.ClubLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ClubLog record.

        Parameters
        ----------
        where
            ClubLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubLog model

        Returns
        -------
        prisma.models.ClubLog
            The found ClubLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clublog = ClubLog.prisma().find_unique(
            where={
                'id': 1855826649,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ClubLogWhereUniqueInput,
        include: Optional[types.ClubLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ClubLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ClubLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubLog model

        Returns
        -------
        prisma.models.ClubLog
            The found ClubLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clublog = ClubLog.prisma().find_unique_or_raise(
            where={
                'id': 1611009182,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubLogWhereInput] = None,
        cursor: Optional[types.ClubLogWhereUniqueInput] = None,
        include: Optional[types.ClubLogInclude] = None,
        order: Optional[Union[types.ClubLogOrderByInput, List[types.ClubLogOrderByInput]]] = None,
        distinct: Optional[List[types.ClubLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ClubLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ClubLog records returned
        skip
            Ignore the first N results
        where
            ClubLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubLog model
        order
            Order the returned ClubLog records by any field
        distinct
            Filter ClubLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ClubLog]
            The list of all ClubLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ClubLog records
        clublogs = ClubLog.prisma().find_many(take=10)

        # find the first 5 ClubLog records ordered by the createdAt field
        clublogs = ClubLog.prisma().find_many(
            take=5,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubLogWhereInput] = None,
        cursor: Optional[types.ClubLogWhereUniqueInput] = None,
        include: Optional[types.ClubLogInclude] = None,
        order: Optional[Union[types.ClubLogOrderByInput, List[types.ClubLogOrderByInput]]] = None,
        distinct: Optional[List[types.ClubLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ClubLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubLog model
        order
            Order the returned ClubLog records by any field
        distinct
            Filter ClubLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubLog
            The first ClubLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubLog record ordered by the logType field
        clublog = ClubLog.prisma().find_first(
            skip=1,
            order={
                'logType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubLogWhereInput] = None,
        cursor: Optional[types.ClubLogWhereUniqueInput] = None,
        include: Optional[types.ClubLogInclude] = None,
        order: Optional[Union[types.ClubLogOrderByInput, List[types.ClubLogOrderByInput]]] = None,
        distinct: Optional[List[types.ClubLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ClubLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubLog model
        order
            Order the returned ClubLog records by any field
        distinct
            Filter ClubLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubLog
            The first ClubLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubLog record ordered by the data field
        clublog = ClubLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'data': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ClubLogUpdateInput,
        where: types.ClubLogWhereUniqueInput,
        include: Optional[types.ClubLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ClubLog record.

        Parameters
        ----------
        data
            ClubLog record data specifying what to update
        where
            ClubLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ClubLog model

        Returns
        -------
        prisma.models.ClubLog
            The updated ClubLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        clublog = ClubLog.prisma().update(
            where={
                'id': 446673791,
            },
            data={
                # data to update the ClubLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ClubLogWhereUniqueInput,
        data: types.ClubLogUpsertInput,
        include: Optional[types.ClubLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ClubLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ClubLog model

        Returns
        -------
        prisma.models.ClubLog
            The created or updated ClubLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clublog = ClubLog.prisma().upsert(
            where={
                'id': 300568396,
            },
            data={
                'create': {
                    'id': 300568396,
                    'clubId': 340946258,
                    'logType': enums.ClubLogType.CLUB_CREATED,
                    'data': Json({'gabahhhjf': True}),
                },
                'update': {
                    'clubId': 340946258,
                    'logType': enums.ClubLogType.CLUB_CREATED,
                    'data': Json({'gabahhhjf': True}),
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ClubLogUpdateManyMutationInput,
        where: types.ClubLogWhereInput,
    ) -> int:
        """Update multiple ClubLog records

        Parameters
        ----------
        data
            ClubLog data to update the selected ClubLog records to
        where
            Filter to select the ClubLog records to update

        Returns
        -------
        int
            The total number of ClubLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ClubLog records
        total = ClubLog.prisma().update_many(
            data={
                'id': 632626069
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubLogWhereInput] = None,
        cursor: Optional[types.ClubLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ClubLog records present in the database

        Parameters
        ----------
        select
            Select the ClubLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubLog.prisma().count()

        # results: prisma.types.ClubLogCountAggregateOutput
        results = ClubLog.prisma().count(
            select={
                '_all': True,
                'clubId': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ClubLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubLogWhereInput] = None,
        cursor: Optional[types.ClubLogWhereUniqueInput] = None,
    ) -> types.ClubLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ClubLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubLogWhereInput] = None,
        cursor: Optional[types.ClubLogWhereUniqueInput] = None,
    ) -> Union[int, types.ClubLogCountAggregateOutput]:
        """Count the number of ClubLog records present in the database

        Parameters
        ----------
        select
            Select the ClubLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubLog.prisma().count()

        # results: prisma.types.ClubLogCountAggregateOutput
        results = ClubLog.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ClubLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ClubLogWhereInput] = None
    ) -> int:
        """Delete multiple ClubLog records.

        Parameters
        ----------
        where
            Optional ClubLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ClubLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ClubLog records
        total = ClubLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ClubLogScalarFieldKeysT'],
        *,
        where: Optional['types.ClubLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ClubLogAvgAggregateInput'] = None,
        sum: Optional['types.ClubLogSumAggregateInput'] = None,
        min: Optional['types.ClubLogMinAggregateInput'] = None,
        max: Optional['types.ClubLogMaxAggregateInput'] = None,
        having: Optional['types.ClubLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ClubLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ClubLogScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.ClubLogScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.ClubLogGroupByOutput']:
        """Group ClubLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ClubLog fields to group records by
        where
            ClubLog filter to select records
        take
            Limit the maximum number of ClubLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ClubLogGroupByOutput]
            A list of dictionaries representing the ClubLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ClubLog records by logType values
        # and count how many records are in each group
        results = ClubLog.prisma().group_by(
            ['logType'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ClubInfractionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ClubInfraction]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ClubInfraction.prisma().query_raw(
            'SELECT * FROM ClubInfraction WHERE id = $1',
            1724011690,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ClubInfraction
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ClubInfraction.prisma().query_first(
            'SELECT * FROM ClubInfraction WHERE clubId = $1',
            470157467,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ClubInfractionCreateInput,
        include: Optional[types.ClubInfractionInclude] = None
    ) -> _PrismaModelT:
        """Create a new ClubInfraction record.

        Parameters
        ----------
        data
            ClubInfraction record data
        include
            Specifies which relations should be loaded on the returned ClubInfraction model

        Returns
        -------
        prisma.models.ClubInfraction
            The created ClubInfraction record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ClubInfraction record from just the required fields
        clubinfraction = ClubInfraction.prisma().create(
            data={
                # data to create a ClubInfraction record
                'clubId': 1209209912,
                'createdById': 1536744465,
                'infractionType': enums.ClubInfractionType.WARNING,
                'reason': 'ececbijji',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ClubInfractionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ClubInfraction records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ClubInfraction record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ClubInfraction.prisma().create_many(
            data=[
                {
                    # data to create a ClubInfraction record
                    'clubId': 2125632375,
                    'createdById': 536951780,
                    'infractionType': enums.ClubInfractionType.WARNING,
                    'reason': 'jcehcdchh',
                },
                {
                    # data to create a ClubInfraction record
                    'clubId': 1621937922,
                    'createdById': 1848832019,
                    'infractionType': enums.ClubInfractionType.WARNING,
                    'reason': 'bjcbfcieaa',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ClubInfractionWhereUniqueInput,
        include: Optional[types.ClubInfractionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ClubInfraction record.

        Parameters
        ----------
        where
            ClubInfraction filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ClubInfraction model

        Returns
        -------
        prisma.models.ClubInfraction
            The deleted ClubInfraction record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubinfraction = ClubInfraction.prisma().delete(
            where={
                'id': 2100427849,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ClubInfractionWhereUniqueInput,
        include: Optional[types.ClubInfractionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ClubInfraction record.

        Parameters
        ----------
        where
            ClubInfraction filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubInfraction model

        Returns
        -------
        prisma.models.ClubInfraction
            The found ClubInfraction record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubinfraction = ClubInfraction.prisma().find_unique(
            where={
                'id': 849140046,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ClubInfractionWhereUniqueInput,
        include: Optional[types.ClubInfractionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ClubInfraction record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ClubInfraction filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubInfraction model

        Returns
        -------
        prisma.models.ClubInfraction
            The found ClubInfraction record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubinfraction = ClubInfraction.prisma().find_unique_or_raise(
            where={
                'id': 928152175,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubInfractionWhereInput] = None,
        cursor: Optional[types.ClubInfractionWhereUniqueInput] = None,
        include: Optional[types.ClubInfractionInclude] = None,
        order: Optional[Union[types.ClubInfractionOrderByInput, List[types.ClubInfractionOrderByInput]]] = None,
        distinct: Optional[List[types.ClubInfractionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ClubInfraction records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ClubInfraction records returned
        skip
            Ignore the first N results
        where
            ClubInfraction filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubInfraction model
        order
            Order the returned ClubInfraction records by any field
        distinct
            Filter ClubInfraction records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ClubInfraction]
            The list of all ClubInfraction records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ClubInfraction records
        clubinfractions = ClubInfraction.prisma().find_many(take=10)

        # find the first 5 ClubInfraction records ordered by the createdAt field
        clubinfractions = ClubInfraction.prisma().find_many(
            take=5,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubInfractionWhereInput] = None,
        cursor: Optional[types.ClubInfractionWhereUniqueInput] = None,
        include: Optional[types.ClubInfractionInclude] = None,
        order: Optional[Union[types.ClubInfractionOrderByInput, List[types.ClubInfractionOrderByInput]]] = None,
        distinct: Optional[List[types.ClubInfractionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ClubInfraction record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubInfraction filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubInfraction model
        order
            Order the returned ClubInfraction records by any field
        distinct
            Filter ClubInfraction records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubInfraction
            The first ClubInfraction record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubInfraction record ordered by the createdById field
        clubinfraction = ClubInfraction.prisma().find_first(
            skip=1,
            order={
                'createdById': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubInfractionWhereInput] = None,
        cursor: Optional[types.ClubInfractionWhereUniqueInput] = None,
        include: Optional[types.ClubInfractionInclude] = None,
        order: Optional[Union[types.ClubInfractionOrderByInput, List[types.ClubInfractionOrderByInput]]] = None,
        distinct: Optional[List[types.ClubInfractionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ClubInfraction record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubInfraction filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubInfraction model
        order
            Order the returned ClubInfraction records by any field
        distinct
            Filter ClubInfraction records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubInfraction
            The first ClubInfraction record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubInfraction record ordered by the infractionType field
        clubinfraction = ClubInfraction.prisma().find_first_or_raise(
            skip=1,
            order={
                'infractionType': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ClubInfractionUpdateInput,
        where: types.ClubInfractionWhereUniqueInput,
        include: Optional[types.ClubInfractionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ClubInfraction record.

        Parameters
        ----------
        data
            ClubInfraction record data specifying what to update
        where
            ClubInfraction filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ClubInfraction model

        Returns
        -------
        prisma.models.ClubInfraction
            The updated ClubInfraction record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        clubinfraction = ClubInfraction.prisma().update(
            where={
                'id': 273032060,
            },
            data={
                # data to update the ClubInfraction record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ClubInfractionWhereUniqueInput,
        data: types.ClubInfractionUpsertInput,
        include: Optional[types.ClubInfractionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ClubInfraction filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ClubInfraction model

        Returns
        -------
        prisma.models.ClubInfraction
            The created or updated ClubInfraction record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubinfraction = ClubInfraction.prisma().upsert(
            where={
                'id': 982848517,
            },
            data={
                'create': {
                    'id': 982848517,
                    'clubId': 1621937922,
                    'createdById': 1848832019,
                    'infractionType': enums.ClubInfractionType.WARNING,
                    'reason': 'bjcbfcieaa',
                },
                'update': {
                    'clubId': 1621937922,
                    'createdById': 1848832019,
                    'infractionType': enums.ClubInfractionType.WARNING,
                    'reason': 'bjcbfcieaa',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ClubInfractionUpdateManyMutationInput,
        where: types.ClubInfractionWhereInput,
    ) -> int:
        """Update multiple ClubInfraction records

        Parameters
        ----------
        data
            ClubInfraction data to update the selected ClubInfraction records to
        where
            Filter to select the ClubInfraction records to update

        Returns
        -------
        int
            The total number of ClubInfraction records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ClubInfraction records
        total = ClubInfraction.prisma().update_many(
            data={
                'expiresAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubInfractionWhereInput] = None,
        cursor: Optional[types.ClubInfractionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ClubInfraction records present in the database

        Parameters
        ----------
        select
            Select the ClubInfraction fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubInfraction filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubInfractionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubInfraction.prisma().count()

        # results: prisma.types.ClubInfractionCountAggregateOutput
        results = ClubInfraction.prisma().count(
            select={
                '_all': True,
                'repealedAt': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ClubInfractionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubInfractionWhereInput] = None,
        cursor: Optional[types.ClubInfractionWhereUniqueInput] = None,
    ) -> types.ClubInfractionCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ClubInfractionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubInfractionWhereInput] = None,
        cursor: Optional[types.ClubInfractionWhereUniqueInput] = None,
    ) -> Union[int, types.ClubInfractionCountAggregateOutput]:
        """Count the number of ClubInfraction records present in the database

        Parameters
        ----------
        select
            Select the ClubInfraction fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubInfraction filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubInfractionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubInfraction.prisma().count()

        # results: prisma.types.ClubInfractionCountAggregateOutput
        results = ClubInfraction.prisma().count(
            select={
                '_all': True,
                'reason': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ClubInfractionCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ClubInfractionWhereInput] = None
    ) -> int:
        """Delete multiple ClubInfraction records.

        Parameters
        ----------
        where
            Optional ClubInfraction filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ClubInfraction records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ClubInfraction records
        total = ClubInfraction.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ClubInfractionScalarFieldKeysT'],
        *,
        where: Optional['types.ClubInfractionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ClubInfractionAvgAggregateInput'] = None,
        sum: Optional['types.ClubInfractionSumAggregateInput'] = None,
        min: Optional['types.ClubInfractionMinAggregateInput'] = None,
        max: Optional['types.ClubInfractionMaxAggregateInput'] = None,
        having: Optional['types.ClubInfractionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ClubInfractionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ClubInfractionScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.ClubInfractionScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.ClubInfractionGroupByOutput']:
        """Group ClubInfraction records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ClubInfraction fields to group records by
        where
            ClubInfraction filter to select records
        take
            Limit the maximum number of ClubInfraction records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ClubInfractionGroupByOutput]
            A list of dictionaries representing the ClubInfraction record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ClubInfraction records by supportingEvidence values
        # and count how many records are in each group
        results = ClubInfraction.prisma().group_by(
            ['supportingEvidence'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ClubModerationLogActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ClubModerationLog]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = ClubModerationLog.prisma().query_raw(
            'SELECT * FROM ClubModerationLog WHERE id = $1',
            510737498,
        )
        ```
        """
        return self._client.query_raw(query, *args, model=self._model)

    def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ClubModerationLog
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = ClubModerationLog.prisma().query_first(
            'SELECT * FROM ClubModerationLog WHERE clubId = $1',
            2117488267,
        )
        ```
        """
        return self._client.query_first(query, *args, model=self._model)

    def create(
        self,
        data: types.ClubModerationLogCreateInput,
        include: Optional[types.ClubModerationLogInclude] = None
    ) -> _PrismaModelT:
        """Create a new ClubModerationLog record.

        Parameters
        ----------
        data
            ClubModerationLog record data
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model

        Returns
        -------
        prisma.models.ClubModerationLog
            The created ClubModerationLog record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ClubModerationLog record from just the required fields
        clubmoderationlog = ClubModerationLog.prisma().create(
            data={
                # data to create a ClubModerationLog record
                'clubId': 1401944936,
                'createdById': 1297607553,
                'logType': enums.ClubModerationLogType.INFRACTION,
                'notes': 'fbjeiiffa',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def create_many(
        self,
        data: List[types.ClubModerationLogCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ClubModerationLog records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ClubModerationLog record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = ClubModerationLog.prisma().create_many(
            data=[
                {
                    # data to create a ClubModerationLog record
                    'clubId': 976832615,
                    'createdById': 1696425492,
                    'logType': enums.ClubModerationLogType.INFRACTION,
                    'notes': 'bgjcgchib',
                },
                {
                    # data to create a ClubModerationLog record
                    'clubId': 1023081650,
                    'createdById': 327681027,
                    'logType': enums.ClubModerationLogType.INFRACTION,
                    'notes': 'fchheijjc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    def delete(
        self,
        where: types.ClubModerationLogWhereUniqueInput,
        include: Optional[types.ClubModerationLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ClubModerationLog record.

        Parameters
        ----------
        where
            ClubModerationLog filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model

        Returns
        -------
        prisma.models.ClubModerationLog
            The deleted ClubModerationLog record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubmoderationlog = ClubModerationLog.prisma().delete(
            where={
                'id': 2029357497,
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def find_unique(
        self,
        where: types.ClubModerationLogWhereUniqueInput,
        include: Optional[types.ClubModerationLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ClubModerationLog record.

        Parameters
        ----------
        where
            ClubModerationLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model

        Returns
        -------
        prisma.models.ClubModerationLog
            The found ClubModerationLog record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubmoderationlog = ClubModerationLog.prisma().find_unique(
            where={
                'id': 1318597118,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    def find_unique_or_raise(
        self,
        where: types.ClubModerationLogWhereUniqueInput,
        include: Optional[types.ClubModerationLogInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ClubModerationLog record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ClubModerationLog filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model

        Returns
        -------
        prisma.models.ClubModerationLog
            The found ClubModerationLog record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubmoderationlog = ClubModerationLog.prisma().find_unique_or_raise(
            where={
                'id': 2122112351,
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubModerationLogWhereInput] = None,
        cursor: Optional[types.ClubModerationLogWhereUniqueInput] = None,
        include: Optional[types.ClubModerationLogInclude] = None,
        order: Optional[Union[types.ClubModerationLogOrderByInput, List[types.ClubModerationLogOrderByInput]]] = None,
        distinct: Optional[List[types.ClubModerationLogScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ClubModerationLog records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ClubModerationLog records returned
        skip
            Ignore the first N results
        where
            ClubModerationLog filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model
        order
            Order the returned ClubModerationLog records by any field
        distinct
            Filter ClubModerationLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ClubModerationLog]
            The list of all ClubModerationLog records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ClubModerationLog records
        clubmoderationlogs = ClubModerationLog.prisma().find_many(take=10)

        # find the first 5 ClubModerationLog records ordered by the clubInfractionId field
        clubmoderationlogs = ClubModerationLog.prisma().find_many(
            take=5,
            order={
                'clubInfractionId': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubModerationLogWhereInput] = None,
        cursor: Optional[types.ClubModerationLogWhereUniqueInput] = None,
        include: Optional[types.ClubModerationLogInclude] = None,
        order: Optional[Union[types.ClubModerationLogOrderByInput, List[types.ClubModerationLogOrderByInput]]] = None,
        distinct: Optional[List[types.ClubModerationLogScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ClubModerationLog record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubModerationLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model
        order
            Order the returned ClubModerationLog records by any field
        distinct
            Filter ClubModerationLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubModerationLog
            The first ClubModerationLog record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubModerationLog record ordered by the createdAt field
        clubmoderationlog = ClubModerationLog.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ClubModerationLogWhereInput] = None,
        cursor: Optional[types.ClubModerationLogWhereUniqueInput] = None,
        include: Optional[types.ClubModerationLogInclude] = None,
        order: Optional[Union[types.ClubModerationLogOrderByInput, List[types.ClubModerationLogOrderByInput]]] = None,
        distinct: Optional[List[types.ClubModerationLogScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ClubModerationLog record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ClubModerationLog filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model
        order
            Order the returned ClubModerationLog records by any field
        distinct
            Filter ClubModerationLog records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ClubModerationLog
            The first ClubModerationLog record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ClubModerationLog record ordered by the createdById field
        clubmoderationlog = ClubModerationLog.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdById': 'desc',
            },
        )
        ```
        """
        resp = self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update(
        self,
        data: types.ClubModerationLogUpdateInput,
        where: types.ClubModerationLogWhereUniqueInput,
        include: Optional[types.ClubModerationLogInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ClubModerationLog record.

        Parameters
        ----------
        data
            ClubModerationLog record data specifying what to update
        where
            ClubModerationLog filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model

        Returns
        -------
        prisma.models.ClubModerationLog
            The updated ClubModerationLog record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        clubmoderationlog = ClubModerationLog.prisma().update(
            where={
                'id': 1024943020,
            },
            data={
                # data to update the ClubModerationLog record to
            },
        )
        ```
        """
        try:
            resp = self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    def upsert(
        self,
        where: types.ClubModerationLogWhereUniqueInput,
        data: types.ClubModerationLogUpsertInput,
        include: Optional[types.ClubModerationLogInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ClubModerationLog filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ClubModerationLog model

        Returns
        -------
        prisma.models.ClubModerationLog
            The created or updated ClubModerationLog record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        clubmoderationlog = ClubModerationLog.prisma().upsert(
            where={
                'id': 1717307509,
            },
            data={
                'create': {
                    'id': 1717307509,
                    'clubId': 1023081650,
                    'createdById': 327681027,
                    'logType': enums.ClubModerationLogType.INFRACTION,
                    'notes': 'fchheijjc',
                },
                'update': {
                    'clubId': 1023081650,
                    'createdById': 327681027,
                    'logType': enums.ClubModerationLogType.INFRACTION,
                    'notes': 'fchheijjc',
                },
            },
        )
        ```
        """
        resp = self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    def update_many(
        self,
        data: types.ClubModerationLogUpdateManyMutationInput,
        where: types.ClubModerationLogWhereInput,
    ) -> int:
        """Update multiple ClubModerationLog records

        Parameters
        ----------
        data
            ClubModerationLog data to update the selected ClubModerationLog records to
        where
            Filter to select the ClubModerationLog records to update

        Returns
        -------
        int
            The total number of ClubModerationLog records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ClubModerationLog records
        total = ClubModerationLog.prisma().update_many(
            data={
                'logType': enums.ClubModerationLogType.INFRACTION
            },
            where={}
        )
        ```
        """
        resp = self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubModerationLogWhereInput] = None,
        cursor: Optional[types.ClubModerationLogWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ClubModerationLog records present in the database

        Parameters
        ----------
        select
            Select the ClubModerationLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubModerationLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubModerationLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubModerationLog.prisma().count()

        # results: prisma.types.ClubModerationLogCountAggregateOutput
        results = ClubModerationLog.prisma().count(
            select={
                '_all': True,
                'notes': True,
            },
        )
        ```
        """


    @overload
    def count(
        self,
        select: types.ClubModerationLogCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubModerationLogWhereInput] = None,
        cursor: Optional[types.ClubModerationLogWhereUniqueInput] = None,
    ) -> types.ClubModerationLogCountAggregateOutput:
        ...

    def count(
        self,
        select: Optional[types.ClubModerationLogCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ClubModerationLogWhereInput] = None,
        cursor: Optional[types.ClubModerationLogWhereUniqueInput] = None,
    ) -> Union[int, types.ClubModerationLogCountAggregateOutput]:
        """Count the number of ClubModerationLog records present in the database

        Parameters
        ----------
        select
            Select the ClubModerationLog fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ClubModerationLog filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ClubModerationLogCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = ClubModerationLog.prisma().count()

        # results: prisma.types.ClubModerationLogCountAggregateOutput
        results = ClubModerationLog.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ClubModerationLogCountAggregateOutput, resp['data']['result']['_count'])

    def delete_many(
        self,
        where: Optional[types.ClubModerationLogWhereInput] = None
    ) -> int:
        """Delete multiple ClubModerationLog records.

        Parameters
        ----------
        where
            Optional ClubModerationLog filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ClubModerationLog records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ClubModerationLog records
        total = ClubModerationLog.prisma().delete_many()
        ```
        """
        resp = self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    def group_by(
        self,
        by: List['types.ClubModerationLogScalarFieldKeysT'],
        *,
        where: Optional['types.ClubModerationLogWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ClubModerationLogAvgAggregateInput'] = None,
        sum: Optional['types.ClubModerationLogSumAggregateInput'] = None,
        min: Optional['types.ClubModerationLogMinAggregateInput'] = None,
        max: Optional['types.ClubModerationLogMaxAggregateInput'] = None,
        having: Optional['types.ClubModerationLogScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ClubModerationLogCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ClubModerationLogScalarFieldKeysT', 'types.SortOrder'], List[Mapping['types.ClubModerationLogScalarFieldKeysT', 'types.SortOrder']]]] = None,
    ) -> List['types.ClubModerationLogGroupByOutput']:
        """Group ClubModerationLog records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ClubModerationLog fields to group records by
        where
            ClubModerationLog filter to select records
        take
            Limit the maximum number of ClubModerationLog records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ClubModerationLogGroupByOutput]
            A list of dictionaries representing the ClubModerationLog record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ClubModerationLog records by clubId values
        # and count how many records are in each group
        results = ClubModerationLog.prisma().group_by(
            ['clubId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models